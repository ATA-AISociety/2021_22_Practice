# Create Python Program that calculates a BMI based on a a user's weight and height

#*Tip : BMI = weight (lb) / height (in) / height (in)] x 703

weight = input("What's your weight?")
height = input("What's yout height?")

wInt = int(weight)
hInt = int(height)

BMI = wInt/(hInt*hInt)

print("Your BMI is: " + str(BMI))